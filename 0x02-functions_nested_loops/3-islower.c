#include "main.h"

/**
 * _islower-execution entry point
 * @c:The argument to evaluate user provided input
 * Return:1 is if loqer is inputed otherwise 0 is returned
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
