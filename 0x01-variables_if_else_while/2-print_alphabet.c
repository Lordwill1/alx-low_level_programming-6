#include <stdio.h>

/**
* main-function main is exetion entry point
* Return: 0
*/

int main(void)
{
	char lowercase = 'a';
	
	
	do {
		putchar(lowercase);
	lowercase++;
		} while (a <= z);
putchar('\n');
	return (0);
}
