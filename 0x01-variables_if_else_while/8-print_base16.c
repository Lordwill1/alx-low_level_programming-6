#include <stdio.h>

/**
 * main-function main is exetion entry point
 * Return: 0
 */

int main(void)
{
	int n;
char hexa;
	
	for (n = 48; n <= 57; n++)
{
putchar(n)

}
	
	
	for (hexa = 'a'; hexa <= 'f'; hexa++)
{
		putchar(hexa);
}
putchar('\n');
	return (0);
}
